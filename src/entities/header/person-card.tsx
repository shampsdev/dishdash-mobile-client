import { View } from 'react-native-reanimated/lib/typescript/Animated';

interface PersonCardProps {
  name: string;
}

export const PersonCard = ({ name }: PersonCardProps) => {
  return <View>
  </View>;
};
